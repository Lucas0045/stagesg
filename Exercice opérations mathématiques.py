from math import pi
rayon = float(input("Quelle est le rayon ? "))
hauteur = float(input("Quelle est la hauteur ? "))
Volume = (pi*rayon*rayon*hauteur)/3.0
print("Le volume du cône droit est de ",Volume , "m3")